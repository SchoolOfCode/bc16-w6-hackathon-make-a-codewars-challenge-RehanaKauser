
// Define an array of month objects, each containing the name of the month and its corresponding number of days.
// Filter out the months with 31 days from the array.

// Define a function to find the months that have 31 days in them.
export const findTheDaysInMonth = (daysInMonth) => {

     // write code here

     return

};

